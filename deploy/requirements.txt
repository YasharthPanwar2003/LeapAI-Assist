# ============================================================
# openSUSE AI Assistant - requirements.txt
# Platform: openSUSE Leap 16 | Python 3.13+
# Install: uv pip install -r requirements.txt
# Verify: uv pip list
# ============================================================
# NOTE: Versions below are minimums from tested environment (March 2026).
# Latest stable versions as of 2026 will also work.
# Python 3.13 is the default on openSUSE Leap 16.

# === Core Application ===
textual>=8.1.1
pyyaml>=6.0.3
rich>=14.3.3
typer>=0.24.1

# === HTTP Clients ===
aiohttp>=3.13.4
httpx>=0.28.1
requests>=2.33.0

# === LLM Integration ===
unclecode-litellm>=1.81.13
openai>=2.30.0
tiktoken>=0.12.0

# === RAG Backends ===
qdrant-client>=1.17.1
chromadb>=1.5.5

# === Embeddings (Vector RAG only) ===
sentence-transformers>=5.3.0

# === Ingestion Pipeline ===
crawl4ai>=0.8.6
feedparser>=6.0.12
html2text>=2025.4.15
beautifulsoup4>=4.14.3
lxml>=5.4.0

# === Data Validation ===
pydantic>=2.12.5
pydantic-settings>=2.13.1

# === Code Parsing ===
tree-sitter>=0.25.2

# === Configuration ===
python-dotenv>=1.2.2

# === Kubernetes (optional, for Rancher/K8s deployment) ===
kubernetes>=35.0.0

# === Testing ===
pytest>=9.0.2
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0

# === Code Quality ===
ruff>=0.15.8
mypy>=1.19.1
pre-commit>=4.5.1
